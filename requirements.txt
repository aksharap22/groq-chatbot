# Core
streamlit>=1.35.0

# LLM Providers — install only what you need, or install all
openai>=1.30.0          # OpenAI (GPT-4o, etc.)
anthropic>=0.30.0       # Anthropic Claude
google-generativeai>=0.7.0  # Google Gemini
groq>=0.9.0             # Groq (fast Llama / Mixtral inference)
